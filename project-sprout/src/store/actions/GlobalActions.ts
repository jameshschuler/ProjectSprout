import { ActionType } from "../types/ActionType";

export const fetching = (fetching: boolean) => ({
  type: ActionType.FETCHING,
  fetching
});

export const error = (fetching: boolean) => ({
  type: ActionType.FETCHING,
  fetching
});

export const success = (fetching: boolean) => ({
  type: ActionType.FETCHING,
  fetching
});
