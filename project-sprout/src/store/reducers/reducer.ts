export const initialState = {
  isLoading: false,
  user: null,
  projects: []
};

export const reducer = (state: any, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};
